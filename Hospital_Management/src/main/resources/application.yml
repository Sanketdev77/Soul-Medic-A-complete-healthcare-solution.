server:
 port: 8080

spring:
 application:
  name: HospitalManagement_Main
   
 eureka:
  instance:
    hostname: localhost  
   
eureka:
 client:
   register-with-eureka: true
   fetch-registry: true
   serviceUrl:
      defaultZone: http://localhost:8761/eureka/